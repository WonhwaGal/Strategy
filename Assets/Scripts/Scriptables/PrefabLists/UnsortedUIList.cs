using Code.UI;
using UnityEngine;

namespace Code.ScriptableObjects
{
    [CreateAssetMenu(fileName = nameof(UnsortedUIList), menuName = "UI/UnsortedUIList")]
    public sealed class UnsortedUIList : UIPrefabSO<UIView>
    {
    }
}